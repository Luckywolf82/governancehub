// This file is intentionally left as a client-side stub.
// The actual implementation lives in /functions/runBaselineAudit.ts (Deno backend).
export async function runBaselineAudit() {
  return { ok: true, message: 'Stub — see /functions/runBaselineAudit.ts' };
}
